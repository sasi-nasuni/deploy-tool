# Frontend Placeholder

Frontend implementation is expected from issue `sasi-nasuni/deploy-tool#2`.

Integration requirements for frontend wiring:
- Configure Vite dev proxy so `/api` targets `http://localhost:8000`.
- Use `npm run dev` for local development and `npm run build` for production assets.
